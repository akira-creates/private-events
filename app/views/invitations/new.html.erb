<h2>Invite Users to <%= @event.name %></h2>
<%= form_with model: [@event, @invitation] do |f| %>
    <%= f.collection_select :user_id, @users, :id, :username %>
    <%= f.submit "Send Invitation" %>
<% end %>